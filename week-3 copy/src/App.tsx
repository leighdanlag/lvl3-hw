import React, { useState, useEffect } from "react";

export const Signup = () => {
  /**
   * 1. Form
   *  - input: username
   *  - input: password
   * 2. Grab the values from the inputs and store them in our component
   *  - useState(s)
   * 3. Submit Button
   *  - store the username, password into local storage, isLoggedIn: "true"
   * 
   * HOMEWORK - Mar 4, 2026
   * 1. make sure username, password is valid before creating user
   * 2. protected route => wrap our dashboard
   *  - validate the user isLoggedIn (flag in localStorage)
   * 3. Login
   *  - prepopulate the username, password in the form fields on component mount
   *  - remember me []
   * 4. Dashboard <div>Welcome, {username}</div>
   *  - store the user in global state: zustand, redux, context api
   *  - { username: chris, password: test, isLoggedIn: true } 
   *  - revalidate the user => global state
   */

  return <></>;
};
